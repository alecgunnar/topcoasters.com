default: &default
  adapter: mysql
  pool: 5
  timeout: 5000

development:
  <<: *default
  database: topcoasters
  username: root
  password: 
  host: localhost

test:
  <<: *default
  database: topcoasters
  username: root
  password: 
  host: localhost

production:
  <<: *default
  database: <%= ENV["DATABASE_NAME"] %>
  username: <%= ENV["DATABASE_USERNAME"] %>
  password: <%= ENV["DATABASE_PASSWORD"] %>
  host: <%= ENV["DATABASE_HOST"] %>
